<Window x:Class="Kil0bitSystemMonitor.ProcessListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Processes"
        Width="620"
        MinWidth="480"
        Height="720"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True"
        ResizeMode="NoResize"
        ShowActivated="True"
        WindowStartupLocation="Manual"
        Opacity="0"
        SnapsToDevicePixels="True"
        KeyDown="Window_KeyDown"
        Deactivated="Window_Deactivated">
    <Border x:Name="RootBorder"
            Background="#E8161616"
            BorderBrush="#55FFFFFF"
            BorderThickness="1"
            CornerRadius="8"
            Padding="0">
        <DockPanel>
            <Border DockPanel.Dock="Top" Padding="12,8,8,8" BorderBrush="#33FFFFFF" BorderThickness="0,0,0,1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock x:Name="TitleText" Text="Processes" FontWeight="SemiBold" FontSize="14" Foreground="#EEEEEE" VerticalAlignment="Center"/>
                    <Button x:Name="OpenTaskMgrBtn" Grid.Column="1" Margin="10,0,0,0" Padding="8,3"
                            Background="#22FFFFFF" BorderBrush="#44FFFFFF" BorderThickness="1"
                            Cursor="Hand" Click="OpenTaskMgrBtn_Click" VerticalAlignment="Center">
                        <TextBlock x:Name="OpenTaskMgrBtnText" Text="Open in Task Manager" FontSize="11" Foreground="#CCFFFFFF"/>
                    </Button>
                    <TextBlock x:Name="StatusText" Grid.Column="3" FontSize="11" Foreground="#88FFFFFF" VerticalAlignment="Center" Margin="12,0,8,0"/>
                    <Button x:Name="CloseBtn" Grid.Column="4" Width="28" Height="28" Padding="0"
                            Background="Transparent" BorderThickness="0" Cursor="Hand"
                            ToolTip="Close" Click="CloseBtn_Click"
                            VerticalAlignment="Center">
                        <TextBlock Text="✕" FontSize="12" Foreground="#CCFFFFFF" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Button>
                </Grid>
            </Border>

            <ListView x:Name="ProcessList"
                      BorderThickness="0"
                      Background="Transparent"
                      Foreground="#EEEEEE"
                      FontSize="12"
                      VirtualizingPanel.IsVirtualizing="True"
                      VirtualizingPanel.VirtualizationMode="Recycling"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                      ScrollViewer.VerticalScrollBarVisibility="Auto"
                      MouseDoubleClick="ProcessList_MouseDoubleClick">
                <ListView.Resources>
                    <Style TargetType="GridViewColumnHeader">
                        <Setter Property="Background" Value="#22FFFFFF"/>
                        <Setter Property="Foreground" Value="#00CCFF"/>
                        <Setter Property="Padding" Value="8,6"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <EventSetter Event="Click" Handler="ColumnHeader_Click"/>
                    </Style>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Padding" Value="0,2"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="#EEEEEE"/>
                    </Style>
                </ListView.Resources>
                <ListView.ContextMenu>
                    <ContextMenu Opened="ContextMenu_Opened" Closed="ContextMenu_Closed">
                        <MenuItem x:Name="MenuEndTask" Header="End task" Click="EndTask_Click"/>
                        <MenuItem x:Name="MenuOpenLocation" Header="Open file location" Click="OpenLocation_Click"/>
                        <MenuItem x:Name="MenuProperties" Header="Properties" Click="Properties_Click"/>
                    </ContextMenu>
                </ListView.ContextMenu>
                <ListView.View>
                    <GridView>
                        <GridViewColumn x:Name="ColIcon" Width="28">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Image Source="{Binding Icon}" Width="16" Height="16"
                                           Stretch="Uniform" SnapsToDevicePixels="True"
                                           RenderOptions.BitmapScalingMode="HighQuality"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn x:Name="ColName" Width="200">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <DockPanel LastChildFill="True">
                                        <Button DockPanel.Dock="Left"
                                                Width="18" Height="18" Padding="0" Margin="0,0,2,0"
                                                Background="Transparent" BorderThickness="0"
                                                Cursor="Hand" Focusable="False"
                                                Visibility="{Binding ExpanderVisibility}"
                                                Click="Expand_Click"
                                                VerticalAlignment="Center">
                                            <TextBlock Text="{Binding ExpanderGlyph}" FontSize="9"
                                                       Foreground="#AAFFFFFF" HorizontalAlignment="Center"/>
                                        </Button>
                                        <TextBlock Text="{Binding DisplayName}"
                                                   Padding="{Binding NamePadding}"
                                                   VerticalAlignment="Center"
                                                   TextTrimming="CharacterEllipsis"/>
                                    </DockPanel>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn x:Name="ColCpu" Width="70" DisplayMemberBinding="{Binding CpuText}"/>
                        <GridViewColumn x:Name="ColMemory" Width="140" DisplayMemberBinding="{Binding MemoryText}"/>
                        <GridViewColumn x:Name="ColDisk" Width="70" DisplayMemberBinding="{Binding DiskText}"/>
                        <GridViewColumn x:Name="ColNetwork" Width="90" DisplayMemberBinding="{Binding NetworkText}"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </DockPanel>
    </Border>
</Window>
